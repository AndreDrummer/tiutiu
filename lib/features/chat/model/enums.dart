enum MessageEnum {
  lastBackgroundImage,
  createdAt,
  receiver,
  sender,
  text,
  id,
}

enum ContactEnum {
  userReceiverReference,
  userSenderReference,
  lastMessageTime,
  userReceiverId,
  userSenderId,
  lastMessage,
  open,
  id,
}

enum ChatActionsEnum {
  dennounceUser,
  deleteChat,
}
