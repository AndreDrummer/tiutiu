enum MessageEnum {
  createdAt,
  receiver,
  sender,
  text,
  id,
}

enum ContactEnum {
  userReceiverReference,
  userSenderReference,
  postTalkingAbout,
  lastMessageTime,
  userReceiverId,
  userSenderId,
  lastMessage,
  open,
  id,
}

enum ChatActionsEnum {
  dennounceUser,
  deleteChat,
}
